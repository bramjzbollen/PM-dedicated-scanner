const fs = require("fs"); const path = require("path"); const sf = path.join(process.env.USERPROFILE, ".openclaw", "workspace-boeboesh", "mission-control-dashboard", "components", "trading", "scalping-auto-trader.tsx"); let s = fs.readFileSync(sf, "utf-8"); const slider = `{/* Max Positions slider */}<div className="p-3 rounded-xl bg-white/[0.02] border border-white/[0.04]"><div className="flex items-center justify-between mb-2"><label className="text-xs font-medium text-white/70">Max Open Positions</label><div className="flex items-center gap-2"><span className="text-lg font-bold font-mono text-violet-400">{config.maxPositions}</span><span className="text-[10px] text-white/30">slots</span></div></div><input type="range" min={1} max={50} step={1} value={config.maxPositions} onChange={e => onUpdateConfig({ maxPositions: Number(e.target.value) })} className="w-full h-2 rounded-lg appearance-none cursor-pointer bg-gradient-to-r from-violet-600/20 via-violet-500/40 to-violet-400/60" /><div className="flex items-center justify-between mt-1.5 text-[9px] text-white/20"><span>1</span><span>50</span></div></div>`; s = s.replace("{/* SL/TP/Trailing sliders */}", slider + "\n              {/* SL/TP/Trailing sliders */}"); fs.writeFileSync(sf, s); console.log("Scalping done");
